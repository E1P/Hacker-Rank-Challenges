const { arrayManipulation } = require("../arrays/array-manipulation");

describe("arrayManipulation", () => {
  it("is a function", () => {
    expect(typeof arrayManipulation).toBe("function");
  });
});
